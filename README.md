# Uniform-Cost-Search
This search algorithm finds a route between any two cities.
It takes three COMMAND-LINE-ARGUMENTS UCS.py input_filename origin_city destination_city
This program always returns the optimal route as long as one exists.

UCS.py is the source code
input.txt contains the list of cities and the distance between them.
